import { PrivateWifi } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const WifiPrivate = createIcon({
	ionic: "use-iconoir",
	iconoir: PrivateWifi,
	material: "wifi_password",
});
