import { ClockRotateRight } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const ClockRotate = createIcon({
	ionic: "use-material",
	iconoir: ClockRotateRight,
	material: "history",
});
