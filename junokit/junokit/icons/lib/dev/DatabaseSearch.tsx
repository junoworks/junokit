import { DatabaseSearch as IconoirDatabaseSearch } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const DatabaseSearch = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirDatabaseSearch,
	material: "database_search",
});
