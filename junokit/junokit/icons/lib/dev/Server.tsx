import { Server as IconoirServer } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Server = createIcon({
	ionic: "use-material",
	iconoir: IconoirServer,
	material: "dns",
});
