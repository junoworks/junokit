import { KanbanBoard } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Kanban = createIcon({
	ionic: "use-material",
	iconoir: KanbanBoard,
	material: "view_kanban",
});
