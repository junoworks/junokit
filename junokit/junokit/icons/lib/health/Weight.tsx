import { WeightAlt } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Weight = createIcon({
	ionic: "use-material",
	iconoir: WeightAlt,
	material: "monitor_weight",
});
