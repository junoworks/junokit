import { Cookie as IconoirCookie } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Cookie = createIcon({
	ionic: "use-material",
	iconoir: IconoirCookie,
	material: "cookie",
});
