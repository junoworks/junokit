import { KeyCommand as IconoirKeyCommand } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const KeyCommand = createIcon({
	ionic: "use-material",
	iconoir: IconoirKeyCommand,
	material: "keyboard_command_key",
});
