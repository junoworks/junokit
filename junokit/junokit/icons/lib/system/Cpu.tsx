import { Cpu as IconoirCpu } from "iconoir-react";
import { hardwareChipOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Cpu = createIcon({
	ionic: hardwareChipOutline,
	iconoir: IconoirCpu,
	material: "memory",
});
