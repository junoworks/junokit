import { Accessibility as IconoirAccessibility } from "iconoir-react";
import { accessibilityOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Accessibility = createIcon({
	ionic: accessibilityOutline,
	iconoir: IconoirAccessibility,
	material: "accessibility_new",
});
