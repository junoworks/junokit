import { Brightness as IconoirBrightness } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Brightness = createIcon({
	ionic: "use-material",
	iconoir: IconoirBrightness,
	material: "brightness_medium",
});
