import { Folder as IconoirFolder } from "iconoir-react";
import { folderOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Folder = createIcon({
	ionic: folderOutline,
	iconoir: IconoirFolder,
	material: "folder",
});
