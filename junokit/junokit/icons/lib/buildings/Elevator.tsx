import { Elevator as IconoirElevator } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Elevator = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirElevator,
	material: "elevator",
});
