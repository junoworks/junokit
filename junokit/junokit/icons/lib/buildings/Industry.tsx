import { Industry as IconoirIndustry } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Industry = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirIndustry,
	material: "factory",
});
