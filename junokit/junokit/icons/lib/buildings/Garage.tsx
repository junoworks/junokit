import { Garage as IconoirGarage } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Garage = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirGarage,
	material: "garage_home",
});
