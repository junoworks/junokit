import { Search as IconoirSearch } from "iconoir-react";
import { searchOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Search = createIcon({
	ionic: searchOutline,
	iconoir: IconoirSearch,
	material: "search",
});
