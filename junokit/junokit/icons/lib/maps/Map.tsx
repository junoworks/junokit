import { Map as IconoirMap } from "iconoir-react";
import { mapOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Map = createIcon({
	ionic: mapOutline,
	iconoir: IconoirMap,
	material: "map",
});
