import { UTurnArrowLeft } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const UTurnLeft = createIcon({
	ionic: "use-material",
	iconoir: UTurnArrowLeft,
	material: "u_turn_left",
});
