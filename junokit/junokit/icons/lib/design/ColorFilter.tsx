import { ColorFilter as IconoirColorFilter } from "iconoir-react";
import { colorFilterOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const ColorFilter = createIcon({
	ionic: colorFilterOutline,
	iconoir: IconoirColorFilter,
	material: "filter_vintage",
});
