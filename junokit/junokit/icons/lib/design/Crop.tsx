import { Crop as IconoirCrop } from "iconoir-react";
import { cropOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Crop = createIcon({
	ionic: cropOutline,
	iconoir: IconoirCrop,
	material: "crop",
});
