import { SortDown } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const FlexCol = createIcon({
	ionic: "use-material",
	iconoir: SortDown,
	material: "flex_direction",
});
