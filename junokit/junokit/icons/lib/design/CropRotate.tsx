import { CropRotateTr } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const CropRotate = createIcon({
	ionic: "use-iconoir",
	iconoir: CropRotateTr,
	material: "crop_rotate",
});
