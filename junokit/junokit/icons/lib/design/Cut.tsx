import { Cut as IconoirCut } from "iconoir-react";
import { cutOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Cut = createIcon({
	ionic: cutOutline,
	iconoir: IconoirCut,
	material: "content_cut",
});
