import { createIcon } from "../../createIcon";

export const JustifyHorCenter = createIcon({
	ionic: "use-material",
	iconoir: "use-material",
	material: { name: "align_center", transform: "rotate(90deg)" },
});
