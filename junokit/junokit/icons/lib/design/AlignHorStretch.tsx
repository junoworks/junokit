import { AlignHorizontalSpacingSolid } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const AlignHorStretch = createIcon({
	ionic: "use-iconoir",
	iconoir: AlignHorizontalSpacingSolid,
	material: { name: "align_items_stretch", filled: true, transform: "rotate(90deg)" },
});
