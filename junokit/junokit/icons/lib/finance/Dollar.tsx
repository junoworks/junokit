import { Dollar as IconoirDollar } from "iconoir-react";
import { logoUsd } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Dollar = createIcon({
	ionic: logoUsd,
	iconoir: IconoirDollar,
	material: "attach_money",
});
