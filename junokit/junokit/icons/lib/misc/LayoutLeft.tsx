import { LayoutLeft as IconoirLayoutLeft } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const LayoutLeft = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirLayoutLeft,
	material: "view_comfy",
});
