import { ViewColumns3 } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Columns3 = createIcon({
	ionic: "use-iconoir",
	iconoir: ViewColumns3,
	material: "view_column",
});
