import { LinkSlash } from "iconoir-react";
import { unlinkOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Unlink = createIcon({
	ionic: unlinkOutline,
	iconoir: LinkSlash,
	material: "link_off",
});
