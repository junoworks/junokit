import { Table2Columns } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Table = createIcon({
	ionic: "use-iconoir",
	iconoir: Table2Columns,
	material: "table",
});
