import { Compress as IconoirCompress } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Compress = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirCompress,
	material: "compress",
});
