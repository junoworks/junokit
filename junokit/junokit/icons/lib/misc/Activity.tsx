import { Activity as IconoirActivity } from "iconoir-react";
import { pulseOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Activity = createIcon({
	ionic: pulseOutline,
	iconoir: IconoirActivity,
	material: "vital_signs",
});
