import { Hashtag as IconoirHashtag } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Hashtag = createIcon({
	ionic: "use-material",
	iconoir: IconoirHashtag,
	material: "tag",
});
