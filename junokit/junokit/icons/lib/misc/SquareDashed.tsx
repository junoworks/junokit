import { SquareDashed as IconoirSquareDashed } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const SquareDashed = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirSquareDashed,
	material: "select",
});
