import { DeCompress as IconoirDeCompress } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const DeCompress = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirDeCompress,
	material: "expand",
});
