import { Cloud as IconoirCloud } from "iconoir-react";
import { cloudOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Cloud = createIcon({
	ionic: cloudOutline,
	iconoir: IconoirCloud,
	material: "cloud",
});
