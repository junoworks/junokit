import { Reduce as IconoirReduce } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Reduce = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirReduce,
	material: "hide",
});
