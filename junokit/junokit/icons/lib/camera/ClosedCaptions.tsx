import { ClosedCaptionsTag } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const ClosedCaptions = createIcon({
	ionic: "use-material",
	iconoir: ClosedCaptionsTag,
	material: "closed_caption",
});
