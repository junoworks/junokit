import { Enlarge as IconoirEnlarge } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Enlarge = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirEnlarge,
	material: "pan_zoom",
});
