import { Expand as IconoirExpand } from "iconoir-react";
import { expandOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Expand = createIcon({
	ionic: expandOutline,
	iconoir: IconoirExpand,
	material: "zoom_out_map",
});
