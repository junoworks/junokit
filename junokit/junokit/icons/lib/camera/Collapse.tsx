import { Collapse as IconoirCollapse } from "iconoir-react";
import { contractOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Collapse = createIcon({
	ionic: contractOutline,
	iconoir: IconoirCollapse,
	material: "zoom_in_map",
});
