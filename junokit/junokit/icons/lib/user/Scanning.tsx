import { Scanning as IconoirScanning } from "iconoir-react";
import { scanOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Scanning = createIcon({
	ionic: scanOutline,
	iconoir: IconoirScanning,
	material: "document_scanner",
});
