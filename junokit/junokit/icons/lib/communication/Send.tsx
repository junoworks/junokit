import { SendDiagonal } from "iconoir-react";
import { paperPlaneOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Send = createIcon({
	ionic: paperPlaneOutline,
	iconoir: SendDiagonal,
	material: "send",
});
