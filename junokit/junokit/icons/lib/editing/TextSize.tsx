import { TextSize as IconoirTextSize } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const TextSize = createIcon({
	ionic: "use-material",
	iconoir: IconoirTextSize,
	material: "format_size",
});
