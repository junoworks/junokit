import { VerticalSplit as IconoirVerticalSplit } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const VerticalSplit = createIcon({
	ionic: "use-material",
	iconoir: IconoirVerticalSplit,
	material: "height",
});
