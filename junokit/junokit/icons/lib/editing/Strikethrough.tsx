import { Strikethrough as IconoirStrikethrough } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Strikethrough = createIcon({
	ionic: "use-material",
	iconoir: IconoirStrikethrough,
	material: "strikethrough_s",
});
