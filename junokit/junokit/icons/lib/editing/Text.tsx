import { Text as IconoirText } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Text = createIcon({
	ionic: "use-material",
	iconoir: IconoirText,
	material: "title",
});
