import { HorizontalMerge as IconoirHorizontalMerge } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const HorizontalMerge = createIcon({
	ionic: "use-material",
	iconoir: IconoirHorizontalMerge,
	material: "horizontal_align_center",
});
