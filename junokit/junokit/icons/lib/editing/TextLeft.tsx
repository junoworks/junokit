import { AlignLeft } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const TextLeft = createIcon({
	ionic: "use-material",
	iconoir: AlignLeft,
	material: "format_align_left",
});
