import { Bold as IconoirBold } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Bold = createIcon({
	ionic: "use-material",
	iconoir: IconoirBold,
	material: "format_bold",
});
