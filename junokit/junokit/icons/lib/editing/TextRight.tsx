import { AlignRight } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const TextRight = createIcon({
	ionic: "use-material",
	iconoir: AlignRight,
	material: "format_align_right",
});
