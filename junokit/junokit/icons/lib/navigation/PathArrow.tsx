import { PathArrow as IconoirPathArrow } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const PathArrow = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirPathArrow,
	material: "use-iconoir",
});
