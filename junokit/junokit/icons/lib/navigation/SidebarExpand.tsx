import { VerticalMerge } from "iconoir-react";
import { gitMergeOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const SidebarExpand = createIcon({
	ionic: gitMergeOutline,
	iconoir: VerticalMerge,
	material: "left_panel_open",
});
