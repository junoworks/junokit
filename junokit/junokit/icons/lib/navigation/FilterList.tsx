import { FilterListCircle } from "iconoir-react";
import { filterCircleOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const FilterList = createIcon({
	ionic: filterCircleOutline,
	iconoir: FilterListCircle,
	material: "filter_list",
});
