import { SidebarCollapse as IconoirSidebarCollapse } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const SidebarCollapse = createIcon({
	ionic: "use-material",
	iconoir: IconoirSidebarCollapse,
	material: "left_panel_close",
});
