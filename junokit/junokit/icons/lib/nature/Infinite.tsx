import { Infinite as IconoirInfinite } from "iconoir-react";
import { infiniteOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Infinite = createIcon({
	ionic: infiniteOutline,
	iconoir: IconoirInfinite,
	material: "all_inclusive",
});
