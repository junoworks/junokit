import { Tree as IconoirTree } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Tree = createIcon({
	ionic: "use-material",
	iconoir: IconoirTree,
	material: "nature",
});
