import { Skateboarding as IconoirSkateboarding } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Skateboarding = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirSkateboarding,
	material: "skateboarding",
});
