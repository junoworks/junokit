import { Running as IconoirRunning } from "iconoir-react";
import { walkOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Running = createIcon({
	ionic: walkOutline,
	iconoir: IconoirRunning,
	material: "directions_run",
});
