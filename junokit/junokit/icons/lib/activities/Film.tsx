import { CinemaOld } from "iconoir-react";
import { filmOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Film = createIcon({
	ionic: filmOutline,
	iconoir: CinemaOld,
	material: "theaters",
});
