import { Movie as IconoirMovie } from "iconoir-react";
import { filmOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Movie = createIcon({
	ionic: filmOutline,
	iconoir: IconoirMovie,
	material: "movie",
});
