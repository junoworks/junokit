import { SeaWaves as IconoirSeaWaves } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const SeaWaves = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirSeaWaves,
	material: "water",
});
