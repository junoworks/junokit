import { SoundMin as IconoirSoundMin } from "iconoir-react";
import { volumeOffOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const SoundMin = createIcon({
	ionic: volumeOffOutline,
	iconoir: IconoirSoundMin,
	material: "volume_mute",
});
