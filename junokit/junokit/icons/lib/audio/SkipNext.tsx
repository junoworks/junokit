import { SkipNext as IconoirSkipNext } from "iconoir-react";
import { playSkipForwardOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const SkipNext = createIcon({
	ionic: playSkipForwardOutline,
	iconoir: IconoirSkipNext,
	material: "skip_next",
});
