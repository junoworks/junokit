import { Headset as IconoirHeadset } from "iconoir-react";
import { headsetOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Headset = createIcon({
	ionic: headsetOutline,
	iconoir: IconoirHeadset,
	material: "headset",
});
