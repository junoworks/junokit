import { SkipPrev as IconoirSkipPrev } from "iconoir-react";
import { playSkipBackOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const SkipPrev = createIcon({
	ionic: playSkipBackOutline,
	iconoir: IconoirSkipPrev,
	material: "skip_previous",
});
