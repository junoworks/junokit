import { Restart as IconoirRestart } from "iconoir-react";
import { refreshOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Restart = createIcon({
	ionic: refreshOutline,
	iconoir: IconoirRestart,
	material: "refresh",
});
