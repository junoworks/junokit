import { PasswordCursor as IconoirPasswordCursor } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const PasswordCursor = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirPasswordCursor,
	material: "use-iconoir",
});
