import { PasswordCheck as IconoirPasswordCheck } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const PasswordCheck = createIcon({
	ionic: "use-material",
	iconoir: IconoirPasswordCheck,
	material: "password",
});
