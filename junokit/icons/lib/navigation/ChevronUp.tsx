import { NavArrowUp } from "iconoir-react";
import { chevronUpOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const ChevronUp = createIcon({
	ionic: chevronUpOutline,
	iconoir: NavArrowUp,
	material: "keyboard_arrow_up",
});
