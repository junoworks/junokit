import { MoreVert as IconoirMoreVert } from "iconoir-react";
import { ellipsisVertical } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const MoreVert = createIcon({
	ionic: ellipsisVertical,
	iconoir: IconoirMoreVert,
	material: "more_vert",
});
