import { ChatLinesSolid as IconoirChatLinesSolid } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const ChatLinesSolid = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirChatLinesSolid,
	material: { name: "chat", filled: true },
});
