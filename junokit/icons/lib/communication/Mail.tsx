import { Mail as IconoirMail } from "iconoir-react";
import { mailOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Mail = createIcon({
	ionic: mailOutline,
	iconoir: IconoirMail,
	material: "mail_outline",
});
