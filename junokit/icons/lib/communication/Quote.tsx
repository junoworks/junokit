import { Quote as IconoirQuote } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Quote = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirQuote,
	material: "format_quote",
});
