import { DatabaseBackup as IconoirDatabaseBackup } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const DatabaseBackup = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirDatabaseBackup,
	material: "database_upload",
});
