import { TerminalTag } from "iconoir-react";
import { terminalOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Linux = createIcon({
	ionic: terminalOutline,
	iconoir: TerminalTag,
	material: "terminal",
});
