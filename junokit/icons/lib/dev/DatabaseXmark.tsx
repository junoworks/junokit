import { DatabaseXmark as IconoirDatabaseXmark } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const DatabaseXmark = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirDatabaseXmark,
	material: "database_off",
});
