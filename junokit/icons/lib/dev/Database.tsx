import { Database as IconoirDatabase } from "iconoir-react";
import { serverOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Database = createIcon({
	ionic: serverOutline,
	iconoir: IconoirDatabase,
	material: "database",
});
