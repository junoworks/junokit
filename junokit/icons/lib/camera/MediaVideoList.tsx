import { MediaVideoList as IconoirMediaVideoList } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const MediaVideoList = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirMediaVideoList,
	material: "video_library",
});
