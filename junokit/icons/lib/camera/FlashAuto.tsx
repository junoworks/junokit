import { AutoFlash } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const FlashAuto = createIcon({
	ionic: "use-iconoir",
	iconoir: AutoFlash,
	material: "flash_auto",
});
