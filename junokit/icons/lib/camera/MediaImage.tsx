import { MediaImage as IconoirMediaImage } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const MediaImage = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirMediaImage,
	material: "image",
});
