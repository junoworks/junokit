import { RefreshCircle as IconoirRefreshCircle } from "iconoir-react";
import { refreshCircleOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const RefreshCircle = createIcon({
	ionic: refreshCircleOutline,
	iconoir: IconoirRefreshCircle,
	material: "cached",
});
