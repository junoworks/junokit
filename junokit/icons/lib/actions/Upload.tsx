import { Upload as IconoirUpload } from "iconoir-react";
import { cloudUploadOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Upload = createIcon({
	ionic: cloudUploadOutline,
	iconoir: IconoirUpload,
	material: "upload",
});
