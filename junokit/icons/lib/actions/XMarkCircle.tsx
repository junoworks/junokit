import { XmarkCircle } from "iconoir-react";
import { closeCircleOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const XMarkCircle = createIcon({
	ionic: closeCircleOutline,
	iconoir: XmarkCircle,
	material: "cancel",
});
