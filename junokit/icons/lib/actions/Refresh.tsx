import { Refresh as IconoirRefresh } from "iconoir-react";
import { refreshOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Refresh = createIcon({
	ionic: refreshOutline,
	iconoir: IconoirRefresh,
	material: "refresh",
});
