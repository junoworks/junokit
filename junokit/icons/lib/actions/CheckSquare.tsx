import { CheckSquare as IconoirCheckSquare } from "iconoir-react";
import { checkboxOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const CheckSquare = createIcon({
	ionic: checkboxOutline,
	iconoir: IconoirCheckSquare,
	material: "check_box",
});
