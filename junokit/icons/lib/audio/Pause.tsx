import { Pause as IconoirPause } from "iconoir-react";
import { pauseOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Pause = createIcon({
	ionic: pauseOutline,
	iconoir: IconoirPause,
	material: "pause",
});
