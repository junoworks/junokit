import { Forward as IconoirForward } from "iconoir-react";
import { playForwardOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Forward = createIcon({
	ionic: playForwardOutline,
	iconoir: IconoirForward,
	material: "fast_forward",
});
