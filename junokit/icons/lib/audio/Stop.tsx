import { stop } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Stop = createIcon({
	ionic: stop,
	iconoir: "use-ionic",
	material: { name: "stop", filled: true },
});
