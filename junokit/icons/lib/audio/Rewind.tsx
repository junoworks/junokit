import { Rewind as IconoirRewind } from "iconoir-react";
import { playBackOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Rewind = createIcon({
	ionic: playBackOutline,
	iconoir: IconoirRewind,
	material: "fast_rewind",
});
