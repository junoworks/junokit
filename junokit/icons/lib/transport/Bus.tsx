import { BusStop } from "iconoir-react";
import { busOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Bus = createIcon({
	ionic: busOutline,
	iconoir: BusStop,
	material: "directions_bus",
});
