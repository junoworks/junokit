import { Train as IconoirTrain } from "iconoir-react";
import { trainOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Train = createIcon({
	ionic: trainOutline,
	iconoir: IconoirTrain,
	material: "train",
});
