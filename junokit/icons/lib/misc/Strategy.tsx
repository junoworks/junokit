import { Strategy as IconoirStrategy } from "iconoir-react";
import { gitNetworkOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Strategy = createIcon({
	ionic: gitNetworkOutline,
	iconoir: IconoirStrategy,
	material: "alt_route",
});
