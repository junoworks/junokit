import { StatsReport as IconoirStatsReport } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const StatsReport = createIcon({
	ionic: "use-material",
	iconoir: IconoirStatsReport,
	material: "assessment",
});
