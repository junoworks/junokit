import { Post as IconoirPost } from "iconoir-react";
import { documentTextOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Post = createIcon({
	ionic: documentTextOutline,
	iconoir: IconoirPost,
	material: "article",
});
