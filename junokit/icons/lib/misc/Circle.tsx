import { Circle as IconoirCircle } from "iconoir-react";
import { ellipseOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Circle = createIcon({
	ionic: ellipseOutline,
	iconoir: IconoirCircle,
	material: "circle",
});
