import { Fish as IconoirFish } from "iconoir-react";
import { fishOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Fish = createIcon({
	ionic: fishOutline,
	iconoir: IconoirFish,
	material: "phishing",
});
