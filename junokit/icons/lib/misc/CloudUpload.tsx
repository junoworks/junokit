import { CloudUpload as IconoirCloudUpload } from "iconoir-react";
import { cloudUploadOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const CloudUpload = createIcon({
	ionic: cloudUploadOutline,
	iconoir: IconoirCloudUpload,
	material: "cloud_upload",
});
