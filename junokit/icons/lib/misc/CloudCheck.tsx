import { CloudSync } from "iconoir-react";
import { cloudDownloadOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const CloudCheck = createIcon({
	ionic: cloudDownloadOutline,
	iconoir: CloudSync,
	material: "cloud_done",
});
