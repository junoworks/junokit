import { CloudDownload as IconoirCloudDownload } from "iconoir-react";
import { cloudDownloadOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const CloudDownload = createIcon({
	ionic: cloudDownloadOutline,
	iconoir: IconoirCloudDownload,
	material: "cloud_download",
});
