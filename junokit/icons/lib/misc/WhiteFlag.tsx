import { WhiteFlag as IconoirWhiteFlag } from "iconoir-react";
import { flagOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const WhiteFlag = createIcon({
	ionic: flagOutline,
	iconoir: IconoirWhiteFlag,
	material: "flag",
});
