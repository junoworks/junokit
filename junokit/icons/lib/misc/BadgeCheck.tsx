import { BadgeCheck as IconoirBadgeCheck } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const BadgeCheck = createIcon({
	ionic: "use-material",
	iconoir: IconoirBadgeCheck,
	material: "verified",
});
