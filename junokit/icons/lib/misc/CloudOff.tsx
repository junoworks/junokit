import { CloudXmark } from "iconoir-react";
import { cloudOfflineOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const CloudOff = createIcon({
	ionic: cloudOfflineOutline,
	iconoir: CloudXmark,
	material: "cloud_off",
});
