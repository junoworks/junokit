import { Walking as IconoirWalking } from "iconoir-react";
import { walkOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Walking = createIcon({
	ionic: walkOutline,
	iconoir: IconoirWalking,
	material: "directions_walk",
});
