import { Percentage as IconoirPercentage } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Percentage = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirPercentage,
	material: "percent",
});
