import { Desk as IconoirDesk } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Desk = createIcon({
	ionic: "use-material",
	iconoir: IconoirDesk,
	material: "desk",
});
