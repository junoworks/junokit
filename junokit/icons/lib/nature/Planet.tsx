import { Planet as IconoirPlanet } from "iconoir-react";
import { planetOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Planet = createIcon({
	ionic: planetOutline,
	iconoir: IconoirPlanet,
	material: "planet",
});
