import { Dna as IconoirDna } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Dna = createIcon({
	ionic: "use-material",
	iconoir: IconoirDna,
	material: "genetics",
});
