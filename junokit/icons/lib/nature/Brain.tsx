import { Brain as IconoirBrain } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Brain = createIcon({
	ionic: "use-material",
	iconoir: IconoirBrain,
	material: "neurology",
});
