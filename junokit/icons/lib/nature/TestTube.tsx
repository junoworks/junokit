import { TestTube as IconoirTestTube } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const TestTube = createIcon({
	ionic: "use-material",
	iconoir: IconoirTestTube,
	material: "labs",
});
