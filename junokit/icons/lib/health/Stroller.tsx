import { Stroller as IconoirStroller } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Stroller = createIcon({
	ionic: "use-material",
	iconoir: IconoirStroller,
	material: "stroller",
});
