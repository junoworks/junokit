import { Male as IconoirMale } from "iconoir-react";
import { maleOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Male = createIcon({
	ionic: maleOutline,
	iconoir: IconoirMale,
	material: "male",
});
