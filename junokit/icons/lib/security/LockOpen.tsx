import { LockSlash } from "iconoir-react";
import { lockOpenOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const LockOpen = createIcon({
	ionic: lockOpenOutline,
	iconoir: LockSlash,
	material: "lock_open",
});
