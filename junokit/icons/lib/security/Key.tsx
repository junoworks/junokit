import { KeyXmark } from "iconoir-react";
import { keyOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Key = createIcon({
	ionic: keyOutline,
	iconoir: KeyXmark,
	material: "key",
});
