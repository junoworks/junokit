import { Filter as IconoirFilter } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Filter = createIcon({
	ionic: "use-material",
	iconoir: IconoirFilter,
	material: "filter_alt",
});
