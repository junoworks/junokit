import { PinSlash } from "iconoir-react";
import { pinOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Pin = createIcon({
	ionic: pinOutline,
	iconoir: PinSlash,
	material: "push_pin",
});
