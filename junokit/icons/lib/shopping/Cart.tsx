import { Cart as IconoirCart } from "iconoir-react";
import { cartOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Cart = createIcon({
	ionic: cartOutline,
	iconoir: IconoirCart,
	material: "shopping_cart",
});
