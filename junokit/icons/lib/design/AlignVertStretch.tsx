import { AlignVerticalSpacingSolid } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const AlignVertStretch = createIcon({
	ionic: "use-iconoir",
	iconoir: AlignVerticalSpacingSolid,
	material: "align_items_stretch",
});
