import { CompAlignLeftSolid } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const AlignLeft = createIcon({
	ionic: "use-iconoir",
	iconoir: CompAlignLeftSolid,
	material: "align_horizontal_left",
});
