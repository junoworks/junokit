import { Copy as IconoirCopy } from "iconoir-react";
import { copyOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Copy = createIcon({
	ionic: copyOutline,
	iconoir: IconoirCopy,
	material: "content_copy",
});
