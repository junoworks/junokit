import { Shop as IconoirShop } from "iconoir-react";
import { storefrontOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Shop = createIcon({
	ionic: storefrontOutline,
	iconoir: IconoirShop,
	material: "store",
});
