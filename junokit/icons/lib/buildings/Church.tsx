import { Church as IconoirChurch } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Church = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirChurch,
	material: "church",
});
