import { Bank as IconoirBank } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const Bank = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirBank,
	material: "account_balance",
});
