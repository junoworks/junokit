import { Wallet as IconoirWallet } from "iconoir-react";
import { walletOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const Wallet = createIcon({
	ionic: walletOutline,
	iconoir: IconoirWallet,
	material: "wallet",
});
