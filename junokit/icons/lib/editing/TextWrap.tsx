import { WrapText } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const TextWrap = createIcon({
	ionic: "use-material",
	iconoir: WrapText,
	material: "format_wrap_text",
});
