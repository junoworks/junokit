import { InputField as IconoirInputField } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const InputField = createIcon({
	ionic: "use-iconoir",
	iconoir: IconoirInputField,
	material: "input",
});
