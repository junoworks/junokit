import { MacOptionKey } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const OptionKey = createIcon({
	ionic: "use-material",
	iconoir: MacOptionKey,
	material: "keyboard_option_key",
});
