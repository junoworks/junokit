import { SystemRestart as IconoirSystemRestart } from "iconoir-react";
import { reloadOutline } from "ionicons/icons";
import { createIcon } from "../../createIcon";

export const SystemRestart = createIcon({
	ionic: reloadOutline,
	iconoir: IconoirSystemRestart,
	material: "restart_alt",
});
