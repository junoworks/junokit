import { MacControlKey } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const ControlKey = createIcon({
	ionic: "use-material",
	iconoir: MacControlKey,
	material: "keyboard_control_key",
});
