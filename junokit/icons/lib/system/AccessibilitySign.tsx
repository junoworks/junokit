import { AccessibilitySign as IconoirAccessibilitySign } from "iconoir-react";
import { createIcon } from "../../createIcon";

export const AccessibilitySign = createIcon({
	ionic: "use-material",
	iconoir: IconoirAccessibilitySign,
	material: "accessible",
});
